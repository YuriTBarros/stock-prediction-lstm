FROM python:3.11-slim

WORKDIR /app

# Instalar dependências
RUN pip install --no-cache-dir flask requests

# Copiar aplicação
COPY discord-webhook-proxy/app.py .

# Expor porta
EXPOSE 9094

# Comando para iniciar
CMD ["python", "app.py"]
